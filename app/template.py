# -*- coding: utf-8 -*-
"""点动服务程序的官方建法说明 + 等价代码（供【程序模板】弹窗显示）。

⚠ 依据现场证据（示教器导出的 CARRY.XPL）：**代码在文件里是结构化指令元素**，
   不是一段可自由粘贴的文本。所以「怎么建」比「抄代码」更重要 ——
   下面同时给出**官方点击流程**与**等价代码**，两者都保留。
"""

JOG_TEMPLATE = """\
=====================================================================
 点动服务程序（常驻）—— 官方建法 + 等价代码
 依据：埃夫特官方《新版程序编辑器 操作手册》142 页 与《C30 RPL 指令手册》
=====================================================================

【0. 先说什么叫"点动服务程序"、它的号怎么定】
  · Modbus **没有**原生点动命令（40101 指令位已穷举到 Bit13，没有任何关节级指令）；
    示教器那 12 个 J± 键走的是控制器内部链路，PC 侧抓不到。
  · 所以点动只能用"控制器上跑一个常驻程序来回执"的办法 —— 这个小程序就叫
    **点动服务程序**。上位机负责算好目标角并置触发位，它负责 WAIT → 读角 → MJOINT → 置回执。
  · **它的程序号由你定**（不是固定的 200，现场根本没有 200 这号）。建完记住号，
    回到上位机 ⑤ 卡【程序清单】里 [添加] 这个号，选中后点 [设为点动服务]。
  · 名字随便你叫，中文英文都行（名字只存在上位机的本地清单里，不下发给机器人）。

【A. 官方点击流程（推荐，全点选，基本不用敲字）】
  1. 任务栏 →【程序】→ 进入程序界面（需编程权限；登录密码 999999）
  2. 左侧【指令栏】展开一级菜单，点击要用的指令 → 它被加进右侧【程序树】
  3. 点程序树里刚加的那行 → 右侧跳【参数】页
  4. 设参数：变量类参数点【选择变量】从候选列表选；
            表达式类参数点【编辑】进表达式编辑器写 → 确认 → 再点【应用】
  5. 点任务栏【保存】
     ⚠ 官方原文：「如不保存到文件，对程序的修改将是暂时的」

  按顺序插入这 5 条：
  ┌─ ① 循环/Loop        → WHILE      判断条件保持默认 true
  │                                   （键入时 ENDWHILE 会自动添加，不要自己敲）
  ├─ ② 基本/Basic       → 赋值 /:=   变量选 fidbus.mtcp_wo_b[0]；表达式写 false
  ├─ ③ 基本/Basic       → WAIT       条件点【编辑】写 fidbus.mtcp_ro_b[0]
  ├─ ④ 运动/Movement    → MJOINT     速度/圆滑过渡/工具/用户坐标系 = 下拉选
  │                                   （v100perc 或 v500 / fine / tool0 / wobj0）
  │                                   **目标点**类型选「固定 POINTJ」，填 6 个表达式：
  │                                     fidbus.mtcp_ro_i[0]/100
  │                                     fidbus.mtcp_ro_i[1]/100
  │                                     fidbus.mtcp_ro_i[2]/100
  │                                     fidbus.mtcp_ro_i[3]/100
  │                                     fidbus.mtcp_ro_i[4]/100
  │                                     fidbus.mtcp_ro_i[5]/100
  ├─ ⑤ 基本/Basic       → 赋值 /:=   变量选 fidbus.mtcp_wo_b[0]；表达式写 true
  └─ ⑥ 基本/Basic       → WAIT       条件点【编辑】写 NOT fidbus.mtcp_ro_b[0]
                                     （★ 等上位机撤触发，防止同一发被重复执行）

  ★★ 最容易错的就是 ③ 那条：WAIT 必须等【读区】mtcp_ro_b[0]。
     若写成【写区】mtcp_wo_b[0] —— 程序第一句刚把它置成 false → 永远等不到，
     程序会**卡死在 WAIT**：上位机「运行」位常亮、40035 完成位恒 0、机器人一动不动。

【B. 完整代码（对照用）—— 推荐这一版（现场实测能加载）】
  ① 先到【变量】页声明 6 个 LREAL 程序变量：`j1 j2 j3 j4 j5 j6`
  ② 主程序体：
      WHILE true DO
          fidbus.mtcp_wo_b[0] := false;
          WAIT(fidbus.mtcp_ro_b[0]);
          j1 := fidbus.mtcp_ro_i[0]/100.0;
          j2 := fidbus.mtcp_ro_i[1]/100.0;
          j3 := fidbus.mtcp_ro_i[2]/100.0;
          j4 := fidbus.mtcp_ro_i[3]/100.0;
          j5 := fidbus.mtcp_ro_i[4]/100.0;
          j6 := fidbus.mtcp_ro_i[5]/100.0;
          MJOINT(POINTJ(j1,j2,j3,j4,j5,j6), v100perc, fine, tool0);
          fidbus.mtcp_wo_b[0] := true;
          WAIT(NOT fidbus.mtcp_ro_b[0]);
      END_WHILE;

【C. 不想声明变量也行（若编译器接受在参数位置直接写表达式）】
      MJOINT(POINTJ(fidbus.mtcp_ro_i[0]/100.0, fidbus.mtcp_ro_i[1]/100.0,
                    fidbus.mtcp_ro_i[2]/100.0, fidbus.mtcp_ro_i[3]/100.0,
                    fidbus.mtcp_ro_i[4]/100.0, fidbus.mtcp_ro_i[5]/100.0),
             v100perc, fine, tool0);

【D. 数据通道（官方手册 15.4.5）】
  上位机 → 机器人：40139~40144 = J1~J6 目标绝对角 ×100（I16）
                   40135 Bit0  = 点动触发（电平）
  机器人 → 上位机：40035 Bit0  = 完成标志（1 表示上一发已到位）
  速度 = v100perc × 全局倍率（40103，即上位机「速度 %」）

【E. 建完后】
  · 记住这个程序的**号**，回上位机 ⑤ 卡 [添加] 它 → 选中 → [设为点动服务]
  · 示教器切【自动】模式（手动 T1/T2 下外部控制信号被禁用）
  · 上位机点【一 键 就 绪】→ 自动加载并运行该程序（它挂在 WAIT 上，不会动）
  · 点 J1~J6 的 ±1° 验证
  · 想先看它会不会动：在 ⑤ 卡用【测试跑】+ 2 秒定时试（强制 5% 低速，到点自停）

【F. 「运行了但点了不动」？—— 2026-09-23 现场实测最高频的一个坑】
  症状：上位机「运行」位**常亮**（程序确实在跑）、`40035 完成位`**始终 0**、零位移。
  ⇒ 程序**卡在 WAIT 那一行上**，触发根本没被接住。
     **不是**通信问题，**不是**没加载，**不是**权限问题 —— 就是 WAIT 的条件不成立。

  原因：WAIT 的**条件变量选错**。要等的是【读区】（上位机写、机器人读）：
        fidbus.mtcp_ro_b[0]      ← ✅ 正确（= 40135.Bit0）
    若误选成【写区】（机器人写、上位机读）：
        fidbus.mtcp_wo_b[0]      ← ❌ 错！程序自己第一句刚把它置成 false，永远等不到

  核对清单（照着过一遍）：
    □ WAIT 的条件是 `fidbus.mtcp_ro_b[0]`（不是 mtcp_wo_b[0]、不是 mtcp_ro_b[1]）
    □ 变量名前缀 `fidbus.` 没漏、方括号下标是 `[0]`
    □ 示教器打开程序看【当前执行行】—— 若停在 WAIT 那行，就是条件不成立
    □ MJOINT 的 6 个表达式用 `fidbus.mtcp_ro_i[0..5]/100`（不是 ro_b、不是 wo_*）
    □ 改完必须点【保存】
=====================================================================
"""
